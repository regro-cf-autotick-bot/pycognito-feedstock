{% set name = "pycognito" %}
{% set version = "2022.11.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: aedadab0431efa473d24d9afbd5d80d73d134494106898062f1a543a145e2358

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - boto3 >=1.10.49
    - envs >=1.3
    - python >=3.6
    - python-jose >=3.2.0
    - requests >=2.22.0

test:
  imports:
    - pycognito
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/pvizeli/pycognito
  license: Apache-2.0
  license_file: LICENSE
  summary: Python class to integrate Boto3's Cognito client so it is easy to login users. With SRP support.
  doc_url: https://github.com/pvizeli/pycognito/blob/master/README.md
  dev_url: https://github.com/pvizeli/pycognito

extra:
  recipe-maintainers:
    - kellpossible
